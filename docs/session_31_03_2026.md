# Notes.

In this session, I update the roadmap, in order to have clear milestones to finish the project.

We will be working with  POD5 is the high-performance format.

Check the SquiggleNet model that translate Raw Nanopore signals.

# Problems.

This file does not came with any type of annotations, regarding the AMR so we need to translate to fasta and
make the match manually.

We need to label the dataset so we need to create a pipeline that can be used to train other scientist to reproduce and label the data
so we can have a robust model.

Develop a script that can check their work, and mantain it consistent.

# Ideas.

1. Implement a pipeline that can do this in C, or bash so we can some read automatically.
2. Create NanoSquiggle-AMR-CNN/src/utils/normalization.py.
3. Implement mad_normalize(signal: np.ndarray) -> np.ndarray.